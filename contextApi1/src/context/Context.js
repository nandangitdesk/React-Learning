import React from "react";

const userContext = React.createContext()

export default userContext

//context ek variable provide karega and usko hum ek wrapper ki tarah use karenge -- "contextProvider" 